<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Black behind the tank and the system bars, like the page's own
         background: nothing flashes white while the page loads, and the
         bars' icons stay light. From API 35 the bars are transparent
         (edge-to-edge) and the root's black shows through them; before
         that these colors paint them. -->
    <style name="Theme.Finsical" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
