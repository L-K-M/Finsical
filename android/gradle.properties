# Launcher icons are rendered with javax.imageio inside the daemon;
# headless keeps it from starting a GUI toolkit (a Dock icon on macOS).
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8 -Djava.awt.headless=true
org.gradle.configuration-cache=true
